name: namer_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.0

dependencies:
  flutter:
    sdk: flutter
  flutter_redux: ^0.10.0
  redux: ^5.0.0
  redux_thunk: ^0.4.0  # Or the latest version
  http: ^0.13.3

dev_dependencies:

flutter:
  uses-material-design: true
  assets:
    - assets/images/logo.png